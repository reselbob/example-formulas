// take input from the user
const A = parseInt(prompt("Enter a positive number: "));
const B = parseInt(prompt("Enter another positive number: "));

const C = A + B;

console.log(`The sum of ${A} + ${B} is ${C}`)
